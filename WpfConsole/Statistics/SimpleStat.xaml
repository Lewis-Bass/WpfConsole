<UserControl x:Class="WpfConsole.Statistics.SimpleStat"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:WpfConsole.Statistics"
			 xmlns:converters="clr-namespace:WpfConsole.Converters"   
			 xmlns:data="clr-namespace:WindowsData;assembly=WindowsData"
			 Style="{DynamicResource StandardUserControl}"
			 mc:Ignorable="d" 
			 d:DesignHeight="45" d:DesignWidth="800">
	<Grid Margin="5">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="45*"/>
			<ColumnDefinition Width="45*"/>
		</Grid.ColumnDefinitions>

		<TextBlock Grid.Column="0" Text="{Binding TextDescription}"/>
		<TextBlock Grid.Column="1" Text="{Binding CalculatedValue}"/>
	</Grid>
</UserControl>
